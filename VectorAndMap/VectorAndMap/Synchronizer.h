#pragma once
#include <iostream>
#include <vector>
#include <map>
class Synchronizer
{
public:
	void synchronizeVectorAndMap(std::vector<int>& vectorInt, std::vector<std::pair<int, int>>& tempVector);
};

